{{>header}}

<div class="body">

    {{>sidebar_left}}

    <div class="container">
        <div class="top-body">
            <div class="Title">What's popular?</div>
            <ul class="popular-posts"> 
                {{#each popularPosts}}
                <li class="popular-content">
                    <div class="captions"></div>
                    <h5><strong>{{title}}</strong></h5>
                    <h6>{{description}}</h6>
                    <div class="profile-img-cap">
                        <img src="{{imgSrc}}">  
                        <span class="room-name">{{roomName}}</span>
                    </div>
                </li>
                {{/each}}
            </ul>
        </div>

        <div class="bottom-body">
            {{#each posts}}
            <button type="button" class="btn post-container">
                <div class="lineh"></div>
                <a href="/home"></a>
                <div class="post-header">
                    <div class="op-pfp"><img id="pfp5" src="{{opImg}}"/></div>
                    <div class="post-info">
                        <div class="op-username" id="op-username">by <a href="/profile">{{username}}</a> • {{date}}</div>
                        <div class="room">
                            <img id="fr1" src="{{roomImg}}"/>
                            <span class="roomname">{{roomName}}</span>
                        </div>
                    </div>
                </div>
                <div class="post-content">
                    <div class="post-title">{{postTitle}}</div>
                    <div class="post-content">{{postContent}}</div>
                </div>
                <div class="post-footer">
                    <div class="votes">
                        <ion-icon name="chevron-up-outline"></ion-icon> <span class="upvote">{{upvote}}</span>
                        <ion-icon name="chevron-down-outline"></ion-icon> <span class="downvote">{{downvote}}</span>
                        <ion-icon name="chatbubble-outline"></ion-icon><span class="replynum">{{replynum}}</span>
                    </div>
                </div>
            </button> 
            {{/each}}
        </div>

    </div>

    <div class="right-body">
        <h3>Popular rooms</h3>
        <div class="Rooms-list">
            {{#each popularRooms}}
            <div class="Rooms-img">
                <img src="{{imgSrc}}">
                <div class="room-name">
                    <strong>{{name}}</strong> <br />
                    <h6>{{members}} members</h6>
                </div>
            </div>
            {{/each}}
        </div>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
<script type="module" src="https://unpkg.com/ionicons@7.1.0/dist/ionicons/ionicons.esm.js"></script>
<script nomodule src="https://unpkg.com/ionicons@7.1.0/dist/ionicons/ionicons.js"></script>


